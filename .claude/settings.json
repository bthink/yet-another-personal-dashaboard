{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | grep -E '\\.tsx?$' | { read -r f; [ -n \"$f\" ] && cd /Users/bartoszfink/dzikieProjekty/yetAnotherPersonalDashboard && pnpm tsc --noEmit 2>&1 || true; }",
            "timeout": 30,
            "statusMessage": "Type-checking..."
          }
        ]
      }
    ]
  }
}
